- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitignore:
    ~/.vimrc:
    ~/.config/alacritty/alacritty.yml: 
        path: 'config/alacritty/alacritty.yml'
        create: true
    ~/.zshrc:
