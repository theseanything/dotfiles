- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.tmux.config:
    ~/.gitignore:
    ~/.vimrc:
    ~/.config/alacritty/alacritty.yml: 
        path: 'config/alacritty/alacritty.yml'
        create: true
    ~/.zshrc:
